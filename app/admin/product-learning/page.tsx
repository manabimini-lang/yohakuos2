import { getActivityHubData, createAdminInsight, createProductHypothesis, transitionInsight, transitionProductHypothesis, updateProductHypothesis, createHypothesisFromInsight, updateInsightReview } from "../activity/actions";

export const dynamic = "force-dynamic";

export default async function ProductLearningPage() {
  const { insights, hypotheses } = await getActivityHubData();
  return <section className="mx-auto max-w-6xl space-y-6">
    <header><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Product Learning</p><h1 className="mt-2 text-2xl font-semibold text-slate-900">製品学習</h1><p className="mt-2 text-sm text-slate-600">匿名インサイトを製品仮説へつなぎ、検証結果を残します。</p></header>
    <div className="grid gap-6 xl:grid-cols-2">
      <form action={createAdminInsight} className="space-y-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-semibold text-slate-800">匿名インサイトを登録</h2><input name="title" required placeholder="インサイトのタイトル" className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /><textarea name="summary" required placeholder="個人が特定できない要約" rows={4} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /><div className="grid gap-3 sm:grid-cols-2"><input name="theme" placeholder="テーマ" className="rounded-xl border border-slate-200 px-3 py-2 text-sm" /><input name="audience" placeholder="対象（例：継続利用者）" className="rounded-xl border border-slate-200 px-3 py-2 text-sm" /></div><div className="grid gap-3 sm:grid-cols-2"><input name="usagePurposes" placeholder="利用目的（カンマ区切り）" className="rounded-xl border border-slate-200 px-3 py-2 text-sm" /><label className="text-[11px] text-slate-500">観測人数<input name="observedCount" type="number" min="0" defaultValue="0" className="mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /></label></div><p className="text-[11px] leading-5 text-amber-700">登録後は匿名化レビュー待ちになります。最低集計人数に達しないインサイトは承認できません。</p><button className="rounded-xl bg-slate-900 px-4 py-2 text-sm font-medium text-white">レビュー待ちで保存</button></form>
      <form action={createProductHypothesis} className="space-y-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-semibold text-slate-800">製品仮説を登録</h2><input name="title" required placeholder="仮説タイトル" className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /><textarea name="problem" required placeholder="解決したい課題" rows={3} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /><textarea name="hypothesis" required placeholder="仮説（もし〜なら、〜になる）" rows={3} className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /><input name="successMetric" placeholder="成功指標" className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /><input name="targetSegment" placeholder="対象セグメント" className="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" /><button className="rounded-xl bg-slate-900 px-4 py-2 text-sm font-medium text-white">バックログに保存</button></form>
    </div>
    <div className="grid gap-6 xl:grid-cols-2"><Collection title="インサイト" items={insights.map((item) => ({ id: item.id, title: item.title, detail: item.summary, status: item.status, secondary: `${item.anonymizationStatus} / ${item.observedCount}人`, edit: <form action={updateInsightReview.bind(null, item.id)} className="mt-3 space-y-2 rounded-lg bg-amber-50 p-3"><div className="grid gap-2 sm:grid-cols-2"><label className="text-[11px] text-slate-500">観測人数<input name="observedCount" type="number" min="0" defaultValue={item.observedCount} className="mt-1 w-full rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs" /></label><label className="text-[11px] text-slate-500">最低人数<input name="minimumGroupSize" type="number" min="1" defaultValue={item.minimumGroupSize} className="mt-1 w-full rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs" /></label></div><textarea name="reviewNotes" defaultValue={item.reviewNotes ?? ""} placeholder="レビュー記録・公開判断の根拠" rows={2} className="w-full rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs" /><button className="rounded-lg bg-slate-800 px-3 py-1.5 text-[11px] text-white">レビュー情報を保存</button></form> }))} renderActions={(item) => <>{item.edit}{item.status === "draft" && <form action={transitionInsight.bind(null, item.id, "review_required")}><button className="text-[11px] text-slate-600 underline">レビューへ</button></form>}{item.status === "review_required" && <form action={transitionInsight.bind(null, item.id, "approved")}><button className="text-[11px] text-emerald-700 underline">承認</button></form>}{item.status === "approved" && <><form action={transitionInsight.bind(null, item.id, "published")}><button className="text-[11px] text-indigo-700 underline">公開</button></form><form action={createHypothesisFromInsight.bind(null, item.id)}><button className="text-[11px] text-violet-700 underline">仮説化</button></form></>}</>} /><Collection title="仮説" items={hypotheses.map((item) => ({ id: item.id, title: item.title, detail: item.successMetric ?? item.problem, status: item.status, edit: <form action={updateProductHypothesis.bind(null, item.id)} className="mt-3 space-y-2 rounded-lg bg-slate-50 p-3"><textarea name="evidence" defaultValue={item.evidence ?? ""} placeholder="検証に使った根拠" rows={2} className="w-full rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs" /><textarea name="result" defaultValue={item.result ?? ""} placeholder="検証結果・学び" rows={2} className="w-full rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-xs" /><button className="rounded-lg bg-slate-800 px-3 py-1.5 text-[11px] text-white">学びを保存</button></form> }))} renderActions={(item) => <>{item.edit}{item.status === "backlog" && <form action={transitionProductHypothesis.bind(null, item.id, "planned")}><button className="text-[11px] text-slate-600 underline">計画へ</button></form>}{item.status === "planned" && <form action={transitionProductHypothesis.bind(null, item.id, "in_progress")}><button className="text-[11px] text-indigo-700 underline">検証開始</button></form>}{item.status === "in_progress" && <form action={transitionProductHypothesis.bind(null, item.id, "completed")}><button className="text-[11px] text-emerald-700 underline">完了</button></form>}</>} /></div>
  </section>;
}

function Collection({ title, items, renderActions }: { title: string; items: { id: string; title: string; detail: string; status: string; secondary?: string; edit?: React.ReactNode }[]; renderActions?: (item: { id: string; title: string; detail: string; status: string; secondary?: string; edit?: React.ReactNode }) => React.ReactNode }) { return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-semibold text-slate-800">{title}</h2><div className="mt-3 divide-y divide-slate-100">{items.length === 0 ? <p className="py-6 text-center text-xs text-slate-400">まだありません。</p> : items.map((item) => <div key={item.id} className="py-3"><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium text-slate-800">{item.title}</p><span className="rounded-full bg-slate-50 px-2 py-1 text-[11px] text-slate-600">{item.status}</span></div><p className="mt-1 line-clamp-2 text-xs leading-5 text-slate-500">{item.detail}</p>{item.secondary && <p className="mt-1 text-[10px] text-amber-700">匿名化：{item.secondary}</p>}<div className="mt-2 flex gap-3">{renderActions?.(item)}</div></div>)}</div></div>; }
