// ===================================================
// YOHAKU Companion — Layout
// ===================================================

export default function CompanionLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}